<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Lattice.ViewModels"
             x:Class="Lattice.Views.Dashboard.DashboardView"
             x:DataType="vm:DashboardViewModel">
    
    <ScrollViewer Background="{DynamicResource BackgroundBrush}">
        <StackPanel Margin="32" Spacing="24">
            <TextBlock Text="总览" Classes="title-large"/>
            
            <ProgressBar IsIndeterminate="True" IsVisible="{Binding IsLoading}"
                         Height="2" HorizontalAlignment="Stretch"/>
            
            <TextBlock Text="{Binding ErrorMessage}" 
                       Foreground="{DynamicResource StatusErrorBrush}"
                       IsVisible="{Binding ErrorMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>
            
            <!-- Stats Grid -->
            <Grid ColumnDefinitions="*,*,*,*" RowDefinitions="Auto">
                <Border Grid.Column="0" Classes="lattice-card" Margin="0,0,8,0">
                    <StackPanel Spacing="8">
                        <TextBlock Text="🖥️" FontSize="24"/>
                        <TextBlock Text="{Binding TotalServers}" FontSize="32" FontWeight="Bold"
                                   Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Text="服务器总数" Foreground="{DynamicResource TextTertiaryBrush}"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="1" Classes="lattice-card" Margin="8,0">
                    <StackPanel Spacing="8">
                        <TextBlock Text="🟢" FontSize="24"/>
                        <TextBlock Text="{Binding RunningServers}" FontSize="32" FontWeight="Bold"
                                   Foreground="{DynamicResource StatusRunningBrush}"/>
                        <TextBlock Text="运行中" Foreground="{DynamicResource TextTertiaryBrush}"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" Classes="lattice-card" Margin="8,0">
                    <StackPanel Spacing="8">
                        <TextBlock Text="👥" FontSize="24"/>
                        <TextBlock Text="{Binding TotalUsers}" FontSize="32" FontWeight="Bold"
                                   Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Text="用户数" Foreground="{DynamicResource TextTertiaryBrush}"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="3" Classes="lattice-card" Margin="8,0,0,0">
                    <StackPanel Spacing="8">
                        <TextBlock Text="🌐" FontSize="24"/>
                        <TextBlock Text="{Binding TotalNodes}" FontSize="32" FontWeight="Bold"
                                   Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Text="节点数" Foreground="{DynamicResource TextTertiaryBrush}"/>
                    </StackPanel>
                </Border>
            </Grid>
            
            <!-- System Info -->
            <Border Classes="lattice-card">
                <StackPanel Spacing="12">
                    <TextBlock Text="系统信息" FontSize="18" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <Grid ColumnDefinitions="120,*">
                        <TextBlock Grid.Column="0" Text="运行时间" Foreground="{DynamicResource TextTertiaryBrush}"/>
                        <TextBlock Grid.Column="1" Text="{Binding Uptime}" Foreground="{DynamicResource TextPrimaryBrush}"/>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
